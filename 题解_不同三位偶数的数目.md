# 不同三位偶数的数目 - 详细题解

## 📌 题目概述

给定一个数字数组 `digits`，计算能组成多少个**三位偶数**，其中**三个数字互不相同**。

### 关键条件
- ✅ 三位数（百位 ≠ 0）
- ✅ 个位是偶数（0, 2, 4, 6, 8）
- ✅ 三个数字必须不同

---

## 🎯 解题思路

**核心想法**：以个位数为枚举对象，对每个偶数分类讨论。

设个位为 `A`，中位为 `B`，百位为 `C`，组成三位数 `CBA`。

### 📊 五种数字组合模式

| 模式 | 示例 | 条件 | 计数 |
|------|------|------|------|
| **AAA** | 222 | A ≠ 0, A出现≥3次 | +1 |
| **ABA** | 242 | A ≠ 0, A出现≥2次 | +(num1-1) |
| **BAA** | 422 | A出现≥2次, B≠0 | +num2或num2-1 |
| **BBA** | 442 | A出现≥2次, B≠0,B出现≥2次 | +(num3-1)或num3 |
| **CBA** | 462 | C≠0,三个数都不同 | +(num2-1)×num2或(num1-2) |

### 📝 辅助变量定义

```
num1 = 不同数字的总个数
num2 = 非0的不同数字个数
num3 = 出现≥2次且非0的不同数字个数
```

---

## 💡 模式详解

### 1️⃣ AAA 模式 - 三位相同

```
个位=中位=百位=A
例如：222, 444, 666, 888
```

**条件**：
- A 必须是偶数且 ≠ 0
- A 在数组中出现 ≥ 3 次

**计数**：满足条件则 +1

---

### 2️⃣ ABA 模式 - 百位和个位相同

```
百位=个位=A, 中位=B(B≠A)
例如：242, 262, 282
```

**条件**：
- A 必须 ≠ 0（百位不能为0）
- A 出现 ≥ 2 次

**计数**：中位可选择其他 (num1-1) 个不同数字

---

### 3️⃣ BAA 模式 - 中位和个位相同

```
中位=个位=A, 百位=B(B≠A,B≠0)
例如：422, 922
```

**条件**：
- A 出现 ≥ 2 次
- 百位 B 必须非0

**计数**：
- 若 A = 0：百位选择非0数字 → +num2
- 若 A ≠ 0：百位选择非0且非A的数字 → +(num2-1)

---

### 4️⃣ BBA 模式 - 百位和中位相同

```
百位=中位=B(B≠A,B≠0), 个位=A
例如：442, 662
```

**条件**：
- A 出现 ≥ 2 次
- B 出现 ≥ 2 次且 B ≠ 0

**计数**：
- 若 A ≠ 0：B可选择其他出现≥2次的非0数字 → +(num3-1)
- 若 A = 0：B可选择所有出现≥2次的非0数字 → +num3

---

### 5️⃣ CBA 模式 - 三位都不同

```
百位=C, 中位=B, 个位=A
C≠B≠A, C≠0
例如：246, 468, 802
```

**条件**：
- 三个数字互不相同
- 百位 C ≠ 0

**计数**：
- 若 A = 0：C从非0数字中选(num2种)，B从剩余中选(num2-1种) → +(num2-1)×num2
- 若 A ≠ 0：C从非0且非A数字中选(num2-1种)，B从剩余中选(num1-2种) → +(num2-1)×(num1-2)

---

## 🔧 代码实现

```cpp
class Solution {
public:
    int totalNumbers(vector<int>& digits) {
        map<int, int> mp;
        int num1 = 0;  // 不同数字总个数
        int num2 = 0;  // 非0的不同数字个数
        int num3 = 0;  // 出现≥2次且非0的不同数字个数
        int ans = 0;

        // 统计数字出现次数
        for (int i = 0; i < digits.size(); i++) {
            mp[digits[i]]++;
        }

        // 计算辅助变量
        for (auto it = mp.begin(); it != mp.end(); it++) {
            num1++;
            if (it->first != 0) num2++;
            if (it->second >= 2 && it->first != 0) num3++;
        }

        // 枚举每个偶数作为个位
        for (auto it = mp.begin(); it != mp.end(); it++) {
            if (it->first % 2 == 0) {  // 只处理偶数
                // AAA 模式
                if (it->first != 0 && it->second >= 3) ans++;

                // ABA 模式
                if (it->first != 0 && it->second >= 2) {
                    ans += (num1 - 1);
                }

                // BAA 模式
                if (it->second >= 2) {
                    if (it->first == 0) {
                        ans += num2;
                    } else {
                        ans += (num2 - 1);
                    }
                }

                // BBA 模式
                if (it->first != 0 && it->second >= 2) {
                    ans += (num3 - 1);
                } else {
                    ans += num3;
                }

                // CBA 模式
                if (it->first == 0) {
                    ans += (num2 - 1) * num2;
                } else {
                    ans += (num2 - 1) * (num1 - 2);
                }
            }
        }

        return ans;
    }
};
```

---

## 📈 复杂度分析

- **时间复杂度**：O(n + k²)，其中 n 是数组长度，k 是不同数字个数（最多10个）
- **空间复杂度**：O(k)，用于存储数字计数

---

## ✨ 思路总结

1. 统计每个数字出现的次数
2. 计算三个关键变量：num1, num2, num3
3. 枚举每个偶数作为个位数
4. 对每个偶数分别计算5种模式的方案数
5. 累加所有方案数

**关键点**：通过分类讨论避免重复计数和遗漏！

---

*记住：这类计数问题的关键就是**找到合适的分类标准**，然后**逐一分析每种情况**！* 🚀
