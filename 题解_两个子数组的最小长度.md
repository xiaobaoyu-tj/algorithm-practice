# 两个子数组的最小长度 - 详细题解

> 对应 LeetCode **1477. 找两个和为目标值且不重叠的子数组**。
>
> 题目编号说明：你给出的函数 `minSumOfLengths` 实际对应 LeetCode 1477，而不是 1447。

## 题目概述

给定一个正整数数组 `arr` 和目标值 `target`，找出两个互不重叠的连续子数组，使它们的元素和都等于 `target`，并使两个子数组长度之和最小。

如果不存在这样的两个子数组，返回 `-1`。

## 解题思路

由于数组中的元素都是正整数，可以使用**滑动窗口**在线性时间内找出所有和为 `target` 的子数组。

### 1. 记录每个起点对应的子数组

定义：

```text
dp[i] = 以 i 为起点、和为 target 的子数组的右端点
```

如果不存在这样的子数组，则 `dp[i] = -1`。

使用左右指针 `[i, j]` 维护当前窗口：

- 窗口和小于 `target`：右指针右移，扩大窗口；
- 窗口和大于 `target`：左指针右移，缩小窗口；
- 窗口和等于 `target`：记录 `dp[i] = j`，然后移动左指针继续寻找。

因为数组元素均为正数，左右指针都只会向右移动，所以这一部分的时间复杂度是 `O(n)`。

### 2. 预处理右侧子数组的最小长度

定义：

```text
minlen[i] = 从下标 i 开始，所有和为 target 的子数组中的最小长度
```

从右向左计算：

- 如果 `dp[i] == -1`，则 `minlen[i] = minlen[i + 1]`；
- 否则用当前子数组长度 `dp[i] - i + 1` 更新最小值。

这样，对于一个左侧子数组 `[i, j]`，只需要查询 `minlen[j + 1]`，就能得到右侧且与它不重叠的最短子数组。

### 3. 枚举左侧子数组并更新答案

对于每个 `dp[i] != -1`：

- 左侧子数组长度为 `j - i + 1`；
- 右侧子数组必须从 `j + 1` 或更后面开始，最短长度为 `minlen[j + 1]`；
- 更新两者长度之和的最小值。

## 正确性说明

- 滑动窗口会枚举出每个可能的起点 `i` 对应的和为 `target` 的窗口，并记录其右端点。
- `minlen[j + 1]` 只考虑起点不小于 `j + 1` 的子数组，因此它与左侧子数组一定不重叠。
- 枚举每个可能的左侧子数组时，都选择其右侧所有合法子数组中的最短者，因此不会错过最优解。
- 所有候选组合都���比较，最终得到的就是两个不重叠目标子数组的最小总长度。

## C++ 代码

```cpp
class Solution {
public:
    int minSumOfLengths(vector<int>& arr, int target) {
        int n = arr.size();
        int ans = -1;

        // dp[i]：以 i 为起点、和为 target 的子数组右端点
        vector<int> dp(n, -1);

        // 滑动窗口寻找所有和为 target 的子数组
        int i = 0, j = 0;
        int windowSum = arr[0];

        while (i < n && j < n) {
            if (windowSum == target) {
                dp[i] = j;
                windowSum -= arr[i];
                ++i;

                // 保证窗口合法，并避免重复处理空窗口
                if (j < i && i < n) {
                    windowSum += arr[i];
                    ++j;
                }
            } else if (windowSum < target) {
                ++j;
                if (j < n) {
                    windowSum += arr[j];
                }
            } else {
                windowSum -= arr[i];
                ++i;

                if (j < i && i < n) {
                    windowSum += arr[i];
                    ++j;
                }
            }
        }

        // minlen[i]：起点不小于 i 的目标子数组的最小长度
        // n + 1 表示不存在合法子数组
        vector<int> minlen(n + 1, n + 1);
        for (int i = n - 1; i >= 0; --i) {
            minlen[i] = minlen[i + 1];
            if (dp[i] != -1) {
                int currentLength = dp[i] - i + 1;
                minlen[i] = min(minlen[i], currentLength);
            }
        }

        // 枚举左侧子数组，寻找右侧不重叠的最短子数组
        for (int i = 0; i < n; ++i) {
            if (dp[i] == -1) {
                continue;
            }

            int j = dp[i];
            int leftLength = j - i + 1;
            int rightLength = minlen[j + 1];

            if (rightLength <= n) {
                int totalLength = leftLength + rightLength;
                if (ans == -1 || totalLength < ans) {
                    ans = totalLength;
                }
            }
        }

        return ans;
    }
};
```

## 复杂度分析

- **时间复杂度**：`O(n)`
  - 滑动窗口中两个指针最多各移动 `n` 次；
  - 构造 `minlen` 和枚举答案也都是 `O(n)`。
- **空间复杂度**：`O(n)`
  - 使用 `dp` 和 `minlen` 两个数组。

## 示例

```text
arr = [3, 2, 2, 4, 3], target = 3
```

和为 `3` 的子数组有：

- `[3]`，长度为 `1`；
- `[3]`，长度为 `1`。

它们不重叠，因此答案为：

```text
1 + 1 = 2
```

## 总结

这道题的关键是将问题拆成两步：

1. 用滑动窗口找出所有和为 `target` 的子数组；
2. 用后缀最小值快速查询某个子数组右侧的最短合法子数组。

滑动窗口负责“找区间”，`minlen` 负责“快速匹配右侧最优区间”，两者结合即可在线性时间内解决问题。
