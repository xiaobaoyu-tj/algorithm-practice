# LeetCode 1401：圆和矩形是否有重叠

## 题目描述

给定一个圆和一个轴对齐矩形：

- 圆心为 `(xCenter, yCenter)`，半径为 `radius`；
- 矩形左下角为 `(x1, y1)`，右上角为 `(x2, y2)`。

判断圆和矩形是否存在公共区域。只要圆与矩形相交、相切，或者圆心在矩形内部，都算作重叠。

## 解题思路

矩形的四条边都是线段。判断圆和矩形是否重叠，可以分为两步：

1. **判断圆心是否在矩形内部**：
   如果满足 `x1 <= xCenter <= x2` 且 `y1 <= yCenter <= y2`，圆心就在矩形内，答案一定为 `true`。

2. **判断圆心到矩形边界的最短距离**：
   - 如果圆心在某条边对应的投影范围内，最短距离就是到这条边的垂直距离；
   - 如果圆心的投影落在边的延长线上，则最近点是对应的矩形顶点，使用两点距离公式计算；
   - 只要最近距离不大于半径，圆就与矩形重叠。

代码分别处理矩形的四条边：`x = x1`、`x = x2`、`y = y1` 和 `y = y2`。在处理每条边时，根据圆心所在的位置选择垂足或顶点作为候选最近点。

## 正确性说明

- 若圆心在矩形内部，矩形包含圆心，因此一定重叠。
- 若圆心不在矩形内部，圆与矩形的最短距离一定出现在矩形边界上。
- 对每条边，代码都根据圆心的投影位置，准确计算了圆心到该边线段的最短距离：投影在线段上时取垂直距离，否则取到相应端点的距离。
- 因此，只要四条边中存在一条边到圆心的距离不超过半径，圆就与矩形相交或相切；如果四条边距离都大于半径，则两者不重叠。

所以算法能够正确判断圆和矩形是否重叠。

## 代码

```cpp
#include <cmath>
#include <cstdlib>

class Solution {
public:
    bool checkOverlap(int radius, int xCenter, int yCenter,
                      int x1, int y1, int x2, int y2) {
        // 圆心在矩形内时，两者一定重叠
        if (xCenter >= x1 && xCenter <= x2 &&
            yCenter >= y1 && yCenter <= y2) {
            return true;
        }

        // x = x1 这条边
        if (yCenter >= y2) {
            double d = std::sqrt(1.0 * (xCenter - x1) * (xCenter - x1) +
                                  1.0 * (yCenter - y2) * (yCenter - y2));
            if (d <= radius) return true;
        } else if (yCenter <= y1) {
            double d = std::sqrt(1.0 * (xCenter - x1) * (xCenter - x1) +
                                  1.0 * (yCenter - y1) * (yCenter - y1));
            if (d <= radius) return true;
        } else if (std::abs(xCenter - x1) <= radius) {
            return true;
        }

        // x = x2 这条边
        if (yCenter >= y2) {
            double d = std::sqrt(1.0 * (xCenter - x2) * (xCenter - x2) +
                                  1.0 * (yCenter - y2) * (yCenter - y2));
            if (d <= radius) return true;
        } else if (yCenter <= y1) {
            double d = std::sqrt(1.0 * (xCenter - x2) * (xCenter - x2) +
                                  1.0 * (yCenter - y1) * (yCenter - y1));
            if (d <= radius) return true;
        } else if (std::abs(xCenter - x2) <= radius) {
            return true;
        }

        // y = y1 这条边
        if (xCenter >= x2) {
            double d = std::sqrt(1.0 * (xCenter - x2) * (xCenter - x2) +
                                  1.0 * (yCenter - y1) * (yCenter - y1));
            if (d <= radius) return true;
        } else if (xCenter <= x1) {
            double d = std::sqrt(1.0 * (xCenter - x1) * (xCenter - x1) +
                                  1.0 * (yCenter - y1) * (yCenter - y1));
            if (d <= radius) return true;
        } else if (std::abs(yCenter - y1) <= radius) {
            return true;
        }

        // y = y2 这条边
        if (xCenter >= x2) {
            double d = std::sqrt(1.0 * (xCenter - x2) * (xCenter - x2) +
                                  1.0 * (yCenter - y2) * (yCenter - y2));
            if (d <= radius) return true;
        } else if (xCenter <= x1) {
            double d = std::sqrt(1.0 * (xCenter - x1) * (xCenter - x1) +
                                  1.0 * (yCenter - y2) * (yCenter - y2));
            if (d <= radius) return true;
        } else if (std::abs(yCenter - y2) <= radius) {
            return true;
        }

        return false;
    }
};
```

## 复杂度分析

- **时间复杂度**：`O(1)`，最多检查矩形的四条边。
- **空间复杂度**：`O(1)`。

## 易错���

- 题目中的矩形是闭区间，圆与矩形**相切**时也应该返回 `true`，因此判断条件必须使用 `<= radius`。
- 不能只判断圆心是否在矩形内，还要处理圆心在矩形外但圆与矩形边界相交的情况。
- 圆心靠近矩形顶点时，最近点是顶点而不是边的垂足，需要使用两点距离公式。
