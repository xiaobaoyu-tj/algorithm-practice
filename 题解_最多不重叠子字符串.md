# 最多不重叠子字符串 - 详细题解

> 对应 LeetCode **1520. 最多不重叠子字符串**。

## 题目概述

给定一个字符串 `s`，找出最多数量的、互不重叠的非空子字符串，使得每个子字符串都满足：

- 子字符串中出现的每个字符，在原字符串中的所有出现位置都包含在该子字符串内。

在满足数量最多的前提下，如果有多种方案，返回总长度最小的方案。答案可以按任意顺序返回。

## 解题思路

这道题可以分成三个步骤：

1. 求出每个字符的首次出现位置和最后出现位置；
2. 枚举每个字符首次出现的位置，构造以它为左端点的最小合法区间；
3. 在所有合法区间中使用动态规划，选择数量最多且总长度最小的互不重叠区间。

由于字符串只包含小写英文字母，也可以使用数组代替哈希表；下面保留原解答的 `map` 写法来说明算法。

## 一、求每个字符的出现范围

定义：

```text
first[c] = 字符 c 第一次出现的位置
last[c]  = 字符 c 最后一次出现的位置
```

如果一个合法子字符串包含字符 `c`，那么它必须覆盖 `[first[c], last[c]]` 的全部位置。

因此，只有当一个位置 `i` 是 `s[i]` 的首次出现位置时，才有必要尝试以 `i` 作为子字符串的起点。

## 二、构造最小合法区间

从某个首次出现位置 `i` 开始，维护当前区间右端点 `right`：

1. 初始 `right = last[s[i]]`；
2. 扫描区间 `[i, right]` 中的字符；
3. 如果遇到字符 `c`，就将 `right` 更新为：

```text
right = max(right, last[c])
```

这样可以保证当前区间包含区间内所有字符的全部出现位置。

但是，如果扫描过程中发现某个字符 `c` 在当前位置之前已经出现过，即：

```text
first[c] < i
```

那么这个区间不可能成为合法子字符串，因为它没有包含字符 `c` 的首次出现位置。此时将该起点标记为无效。

最终得到：

```text
least[i] = 以 i 为起点的最小合法子字符串右端点
```

如果不存在合法区间，则 `least[i] = -1`。

### 为什么只需要考虑最小合法区间？

对于同一个起点，如果一个更大的区间也是合法的，那么它不会比最小合法区间带来更多子字符串数量，反而可能导致与其他区间重叠。因此，在同一起点下只保留最小合法区间即可。

## 三、动态规划选择区间

定义两个状态：

```text
dp1[i] = 选择从 i 开始的合法区间后，最多能得到的子字符串数量
         = 1 + dp2[least[i] + 1]

dp2[i] = 从下标 i 开始往后考虑，最多能得到的子字符串数量
```

从右向左计算：

- 如果 `i` 不是合法区间起点，则跳过它；
- 如果 `i` 是合法区间起点，可以选择当前区间，也可以跳过当前位置；
- 优先选择数量更多的方案。

在本题中，若两种方案的子字符串数量相同，选择更短的总长度即可。原解答通过优先使用每个起点对应的**最小合法区间**，并在数量相同的时候保留跳过当前位置的方案，得到符合要求的结果。

## 四、如何恢复答案

定义：

```text
next[i] = 从 i 开始的最优方案中，下一个被选择的区间起点
```

从 `0` 开始：

1. 读取 `next[ind]`，得到当前区间起点；
2. 加入 `s.substr(start, least[start] - start + 1)`；
3. 将 `ind` 移动到当前区间右端点之后；
4. 重复直到没有可选区间。

## C++ 代码

```cpp
class Solution {
public:
    vector<string> maxNumOfSubstrings(string s) {
        int n = s.size();
        map<char, int> first, last;
        map<char, int> seenFromLeft, seenFromRight;

        // 统计每个字符的首次出现位置
        for (int i = 0; i < n; ++i) {
            if (seenFromLeft[s[i]] == 0) {
                first[s[i]] = i;
                seenFromLeft[s[i]]++;
            }
        }

        // 统计每个字符的最后出现位置
        for (int i = n - 1; i >= 0; --i) {
            if (seenFromRight[s[i]] == 0) {
                last[s[i]] = i;
                seenFromRight[s[i]]++;
            }
        }

        // least[i]：以 i 为起点的最小合法区间右端点
        // -1 表示不存在合法区间
        vector<int> least(n + 1, -1);

        for (int i = 0; i < n; ++i) {
            if (first[s[i]] != i) {
                continue;
            }

            int right = last[s[i]];
            map<char, int> used;
            least[i] = right;

            for (int j = i; j <= right; ++j) {
                if (used[s[j]] == 0) {
                    // 当前区间没有覆盖该字符的首次出现位置
                    if (first[s[j]] != i && first[s[j]] != j) {
                        least[i] = -1;
                        break;
                    }
                    used[s[j]]++;
                    right = max(right, last[s[j]]);
                    least[i] = right;
                }
            }
        }

        // dp1[i]：选择 i 开始的区间时，最多可选的区间数
        // dp2[i]：从 i 开始往后考虑时，最多可选的区间数
        vector<int> dp1(n + 1, 0);
        vector<int> dp2(n + 1, 0);
        vector<int> nextStart(n + 1, -1);

        for (int i = n - 1; i >= 0; --i) {
            if (least[i] == -1) {
                dp2[i] = dp2[i + 1];
                nextStart[i] = nextStart[i + 1];
                continue;
            }

            int right = least[i];
            dp1[i] = 1 + dp2[right + 1];

            if (dp1[i] > dp2[i + 1]) {
                dp2[i] = dp1[i];
                nextStart[i] = i;
            } else {
                dp2[i] = dp2[i + 1];
                nextStart[i] = nextStart[i + 1];
            }
        }

        vector<string> ans;
        int index = 0;

        while (index < n) {
            int start = nextStart[index];
            if (start == -1) {
                break;
            }

            ans.push_back(s.substr(start, least[start] - start + 1));
            index = least[start] + 1;
        }

        return ans;
    }
};
```

## 代码中的关键细节

### 1. 为什么扫描时要动态扩大右端点？

例如当前区间中出现了字符 `b`，而 `b` 的最后出现位置比当前右端点更靠后，那么必须扩大区间，否则无法覆盖 `b` 的所有出现位置。

### 2. 为什么遇到 `first[s[j]] < i` 时区间无效？

因为当前候选区间从 `i` 开始，却遗漏了该字符更早的出现位置。根据题目要求，这种区间不满足“包含字符的全部出现位置”。

### 3. 为什么优先最大化数量？

题目首先要求子字符串数量最多，只有在数量相同的情况下才要求总长度最小。因此动态规划首先比较区间数量。

## 复杂度分析

设 `n` 为字符串长度，字符集大小为 `K`。本题中 `K = 26`。

- **时间复杂度**：`O(nK)`，由于 `K` 为常数，也可视为 `O(n)`；
- **空间复杂度**：`O(n + K)`。

如果使用大小为 26 的数组替代 `map`，常数会更小，代码也更简洁。

## 思路总结

1. 记录每个字符的首次和最后出现位置；
2. 只从字符首次出现的位置尝试构造区间；
3. 动态扩展右边界，得到每个起点的最小合法区间；
4. 用动态规划选择最多的互不重叠区间；
5. 通过 `nextStart` 恢复具体答案。

这道题的核心是：**先把每个可能的子字符串压缩成最小合法区间，再把问题转化为区间选择问题。**
